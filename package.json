{
  "name": "core-defense",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Uma unidade central que atira em inimigos que se aproximam.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
